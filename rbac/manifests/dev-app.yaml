apiVersion: apps/v1
kind: Deployment
metadata:
  name: dev-app
  namespace: development
  labels:
    app: dev-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: dev-app
  template:
    metadata:
      labels:
        app: dev-app
    spec:
      containers:
      - name: nginx
        image: nginx:1.21
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: dev-app-service
  namespace: development
spec:
  selector:
    app: dev-app
  ports:
  - port: 80
    targetPort: 80
  type: ClusterIP
